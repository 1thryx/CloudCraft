apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudcrafter
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/cloudcrafter/platform.git'
    targetRevision: HEAD
    path: .
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: cloudcrafter
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true