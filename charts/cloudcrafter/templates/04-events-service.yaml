apiVersion: v1
kind: Service
metadata:
  name: events-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: events
    tier: api
spec:
  selector:
    app: events
    tier: api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: {{ .Values.services.events.port }}