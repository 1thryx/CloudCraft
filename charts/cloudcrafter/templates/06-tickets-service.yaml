apiVersion: v1
kind: Service
metadata:
  name: tickets-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: tickets
    tier: api
spec:
  selector:
    app: tickets
    tier: api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: {{ .Values.services.tickets.port }}