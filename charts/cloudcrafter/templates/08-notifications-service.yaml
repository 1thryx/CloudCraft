apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: notifications
    tier: api
spec:
  selector:
    app: notifications
    tier: api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: {{ .Values.services.notifications.port }}